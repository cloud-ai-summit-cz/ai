# ============================================================================
# Azure OpenAI Configuration
# ============================================================================
# Get these values from your Azure OpenAI deployment

AZURE_OPENAI_ENDPOINT=https://<your-resource>.cognitiveservices.azure.com/

# Model deployment name (gpt-5)
MODEL_DEPLOYMENT_NAME=gpt-5

# API version (use "preview" for Responses API)
AZURE_OPENAI_API_VERSION=preview

# ============================================================================
# MCP Business Registry Configuration
# ============================================================================

# URL of the MCP Business Registry server (MCP endpoint, typically /mcp path)
# For local development, use the direct Container App URL
# For cloud deployment, use the APIM gateway URL (recommended)
MCP_BUSINESS_REGISTRY_URL=https://your-apim-gateway.azure-api.net/business-registry/mcp

# API key for MCP Business Registry authentication (Bearer token)
# This should match the mcp_auth_token used by the MCP services
MCP_BUSINESS_REGISTRY_API_KEY=your-mcp-api-key

# ============================================================================
# MCP Scratchpad Configuration (Optional - for session-scoped collaboration)
# ============================================================================

# URL of the MCP Scratchpad server (for cross-agent collaboration)
# Leave empty to disable scratchpad integration
MCP_SCRATCHPAD_URL=https://your-apim-gateway.azure-api.net/scratchpad/mcp

# API key for MCP Scratchpad authentication (Bearer token)
MCP_SCRATCHPAD_API_KEY=your-mcp-api-key

# ============================================================================
# A2A Server Configuration
# ============================================================================

# Server binding (0.0.0.0 to listen on all interfaces)
A2A_SERVER_HOST=0.0.0.0
A2A_SERVER_PORT=8021

# Public host for AgentCard URL (the address clients will connect to)
# Use "localhost" for local dev, or your actual hostname/IP for remote access
A2A_PUBLIC_HOST=localhost

# API key for authentication (required for production!)
# Clients must provide this via "Authorization: Bearer <key>" or "X-API-Key: <key>" header
# Leave empty or comment out to disable authentication (NOT RECOMMENDED)
A2A_API_KEY=your-secret-api-key-here

# Agent metadata (shown in AgentCard)
A2A_AGENT_NAME=Competitor Analyst Agent
A2A_AGENT_DESCRIPTION=Competitive landscape analysis specialist for Cofilot's coffee business expansion
A2A_AGENT_VERSION=1.0.0

# ============================================================================
# Notes
# ============================================================================
# 
# This agent uses:
# 1. MCP Business Registry - for company data, profiles, financials
# 2. MCP Scratchpad - for session-scoped collaboration with other agents
# 3. Grounded Web Search (Bing) - built-in via HostedWebSearchTool
#
# Unlike mcp-web-search, the web search capability uses Azure OpenAI's 
# Grounding with Bing feature, which is integrated directly with the model.
