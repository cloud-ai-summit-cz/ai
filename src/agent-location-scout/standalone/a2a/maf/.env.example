# ============================================================================
# Azure OpenAI Configuration
# ============================================================================
# Get these values from your Azure OpenAI deployment

AZURE_OPENAI_ENDPOINT=https://<your-resource>.cognitiveservices.azure.com/

# Model deployment name (gpt-5)
MODEL_DEPLOYMENT_NAME=gpt-5

# API version (use "preview" for Responses API)
AZURE_OPENAI_API_VERSION=preview

# ============================================================================
# MCP Scratchpad Configuration (for session-scoped collaboration)
# ============================================================================

# URL of the MCP Scratchpad server (for cross-agent collaboration)
MCP_SCRATCHPAD_URL=https://your-apim-gateway.azure-api.net/scratchpad/mcp

# API key for MCP Scratchpad authentication (Bearer token)
MCP_SCRATCHPAD_API_KEY=your-mcp-api-key

# ============================================================================
# MCP Government Data Configuration (permits, regulations, zoning)
# ============================================================================

# URL of the MCP Government Data server
MCP_GOVERNMENT_DATA_URL=https://your-apim-gateway.azure-api.net/government-data/mcp

# API key for MCP Government Data authentication (Bearer token)
MCP_GOVERNMENT_DATA_API_KEY=your-mcp-api-key

# ============================================================================
# MCP Demographics Configuration (population, income, foot traffic)
# ============================================================================

# URL of the MCP Demographics server
MCP_DEMOGRAPHICS_URL=https://your-apim-gateway.azure-api.net/demographics/mcp

# API key for MCP Demographics authentication (Bearer token)
MCP_DEMOGRAPHICS_API_KEY=your-mcp-api-key

# ============================================================================
# MCP Real Estate Configuration (property listings, rental rates)
# ============================================================================

# URL of the MCP Real Estate server
MCP_REAL_ESTATE_URL=https://your-apim-gateway.azure-api.net/real-estate/mcp

# API key for MCP Real Estate authentication (Bearer token)
MCP_REAL_ESTATE_API_KEY=your-mcp-api-key

# ============================================================================
# A2A Server Configuration
# ============================================================================

# Server binding (0.0.0.0 to listen on all interfaces)
A2A_SERVER_HOST=0.0.0.0
A2A_SERVER_PORT=8023

# Public host for AgentCard URL (the address clients will connect to)
# Use "localhost" for local dev, or your actual hostname/IP for remote access
A2A_PUBLIC_HOST=localhost

# API key for authentication (required for production!)
# Clients must provide this via "Authorization: Bearer <key>" or "X-API-Key: <key>" header
# Leave empty or comment out to disable authentication (NOT RECOMMENDED)
A2A_API_KEY=your-secret-api-key-here

# Agent metadata (shown in AgentCard)
A2A_AGENT_NAME=Location Scout Agent
A2A_AGENT_DESCRIPTION=Location scouting specialist for Cofilot's coffee business expansion
A2A_AGENT_VERSION=1.0.0

# ============================================================================
# Notes
# ============================================================================
# 
# This agent uses:
# 1. Azure OpenAI Responses API with DefaultAzureCredential (Managed Identity)
# 2. MCP Government Data for permits, licenses, regulations, and zoning
# 3. MCP Demographics for population data, income levels, and foot traffic
# 4. MCP Real Estate for property listings and rental rates
# 5. MCP Scratchpad for session-scoped collaboration with other agents
# 6. Grounded Web Search (Bing) for real-time location intelligence
#
# For local development:
# - Login with Azure CLI: az login
# - Copy this file to .env and fill in values
# - Run: uv run main.py
#
# For container deployment:
# - Use managed identity for Azure OpenAI (AZURE_CLIENT_ID)
# - Set PROMPTS_DIR_OVERRIDE=/app/prompts
# - Configure MCP URLs to point to APIM gateway
