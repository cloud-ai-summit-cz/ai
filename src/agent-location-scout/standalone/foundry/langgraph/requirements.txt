# Container runtime dependencies for the hosted agent
# This file is used by the Dockerfile to avoid dependency conflicts
# with azure-ai-projects 2.x (needed for provision.py but conflicts with agentserver)

# LangGraph stack
langgraph>=0.2
langchain>=0.3
langchain-openai>=0.3

# Azure AI Agent Server adapter (brings azure-ai-projects 1.x transitively)
azure-ai-agentserver-langgraph>=1.0.0b1

# Azure authentication
azure-identity>=1.19.0

# Observability
azure-monitor-opentelemetry>=1.6

# Configuration
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
